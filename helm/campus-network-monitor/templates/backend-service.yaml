apiVersion: v1
kind: Service
metadata:
  name: {{ include "cnm.fullname" . }}-backend
spec:
  selector:
    app: {{ include "cnm.fullname" . }}-backend
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: {{ .Values.backend.service.port }}
      protocol: TCP
