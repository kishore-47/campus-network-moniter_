apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: campus-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: campus-frontend
                port:
                  number: 80
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: campus-backend
                port:
                  number: 5000
